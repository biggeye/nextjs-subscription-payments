// types.ts
export type GalleryImage = {
    content_id: string;
    url: string;
    prompt: string;
  };

  
  // types.ts
export type GalleryScript = {
    content_id: string;
    content: string;
    prompt: string;
  };

  